"use strict";let urlString=document.location.search,urlParams=new URLSearchParams(urlString),currentPage=urlParams.get("page");const articles=document.querySelectorAll(".js_article"),links=document.querySelectorAll(".js_sub-menu-list li");function changeArticle(){urlString=document.location.search,urlParams=new URLSearchParams(urlString),currentPage=urlParams.get("page"),articles.forEach((e=>{e.classList.remove("article_active"),e.dataset.name==currentPage&&e.classList.add("article_active")})),links.forEach((e=>{e.classList.remove("sub-menu__item_active"),e.dataset.name==currentPage&&e.classList.add("sub-menu__item_active")}))}links.forEach((e=>{e.addEventListener("click",(a=>{a.preventDefault,history.pushState(null,null,e.dataset.article),changeArticle(),window.scrollTo(0,0)}))})),changeArticle();